# Copy this file to .env or .env.local (both are git-ignored) to override the
# checkout URL locally without touching the production default in src/App.tsx.
#
# Example: point the CTA buttons at a Stripe Payment Link while testing.
VITE_CHECKOUT_URL=https://buy.stripe.com/your_test_link
